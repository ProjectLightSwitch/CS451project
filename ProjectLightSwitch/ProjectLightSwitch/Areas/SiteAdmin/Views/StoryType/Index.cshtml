@model IEnumerable<ProjectLightSwitch.Models.StoryType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StoryTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StoryTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StoryTypeId })
        </td>
    </tr>
}

</table>
